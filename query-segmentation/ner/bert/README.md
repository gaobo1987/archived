This folder contains scripts for training BERT-based NER models.

The trained models can be found in the /models folder 
with its corresponding languages, 
e.g. [../qsegmt/nl/models](../../qsegmt/nl/models).
